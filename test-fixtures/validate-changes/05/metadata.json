{
    "description": "Should return true if there's a change in subfields",
    "comment": "true: we do have (valid) changes in record",
    "expectedResult": true,
    "expectedToThrow": false,
    "enabled": true
  }